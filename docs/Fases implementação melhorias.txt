

 ## Plano de Ações Completo — Sequenciado por Dependência e Impacto

Cada ação é derivada diretamente do relatório. Agrupei em fases lógicas onde ações dentro de uma mesma fase podem ser paralelizadas, mas as fases devem ser executadas em ordem.

---

### Fase 0 — Correções Triviais (< 1 hora cada, risco zero)

| # | Ação | Origem no Relatório | Ficheiro(s) |
|---|------|---------------------|-------------|
| 0.1 | Corrigir placeholder URL `YOUR_USERNAME` | Code Smells | menu_manager.py |
| 0.2 | Corrigir placeholder email `placeholder@example.com` | Segurança | pyproject.toml |
| 0.3 | Resolver 3 TODOs pendentes (implementar ou remover) | Code Smells | `project_view_model.py`, `analysis_control_view_model.py`, `detector.py` |

---

### Fase 1 — Eliminar Exceções Silenciosas (Desbloqueia debugging)

Esta fase é pré-requisito para todas as outras: sem ela, qualquer refatoração futura pode introduzir bugs invisíveis.

| # | Ação | Contagem | Ficheiro(s) Prioritários |
|---|------|----------|--------------------------|
| 1.1 | Substituir `except…pass` por `logger.debug("...", exc_info=True)` nos 14 casos de `state_synchronizer.py` | 14 instâncias | state_synchronizer.py |
| 1.2 | Idem para `gui.py` (~10 instâncias) | ~10 | gui.py |
| 1.3 | Idem para `window_utils.py` (6 instâncias) | 6 | window_utils.py |
| 1.4 | Idem para `widget_factory.py` (4 instâncias) | 4 | widget_factory.py |
| 1.5 | Idem para `hardware_benchmark.py` (7 instâncias) | 7 | hardware_benchmark.py |
| 1.6 | Idem para `__main__.py` (3 instâncias) | 3 | \_\_main\_\_.py |
| 1.7 | Auditar e corrigir as ~50 instâncias restantes espalhadas pelo codebase | ~50 | Diversos |
| 1.8 | Criar regra Ruff customizada ou pre-commit hook para impedir novos `except…pass` sem justificativa | — | pyproject.toml / .pre-commit-config.yaml |

**Total: ~94 instâncias. Meta: 0 `pass` silencioso sem logging.**

---

### Fase 2 — Estreitar Exception Catches Genéricos

Depende da Fase 1 (agora que os silenciosos estão logando, sabemos quais exceptions realmente ocorrem).

| # | Ação | Ficheiro(s) Prioritários |
|---|------|--------------------------|
| 2.1 | Substituir `except Exception` por exceções específicas em `ui_coordinator.py` (17 instâncias) | ui_coordinator.py |
| 2.2 | Idem para `project_view_manager.py` (14 instâncias) | project_view_manager.py |
| 2.3 | Idem para `state_synchronizer.py` (14 instâncias) | state_synchronizer.py |
| 2.4 | Idem para `window_utils.py` (12 instâncias) | window_utils.py |
| 2.5 | Idem para `widget_factory.py` (11 instâncias) | widget_factory.py |
| 2.6 | Idem para `gui.py` (16 instâncias) | gui.py |
| 2.7 | Varrer os 130+ restantes, priorizando coordination layer e core services | Codebase inteiro |

**Meta: Cada `except Exception` remanescente deve ter uma justificativa documentada em comentário.**

---

### Fase 3 — Unificação Estrutural (Elimina inconsistências arquiteturais)

Estas ações são pré-requisito para a decomposição de god classes (Fase 4), pois definem a base de herança correta.

| # | Ação | Detalhe |
|---|------|---------|
| 3.1 | **Unificar `BaseCoordinator`** — manter base_coordinator.py (concreto, mais maduro). Migrar coordinators que herdam de base.py (ABC). Deletar base.py. | 2 ficheiros → 1 |
| 3.2 | **Remover pacote `orchestrators/` legado** — migrar `ui_state_controller.py` e `video_processing_orchestrator.py` para `coordinators/` ou eliminar se já substituídos | 2 ficheiros |
| 3.3 | **Remover campos `DEPRECATED` de `MainViewModelDependencies`** — campos como `analysis_coordinator: Any = None` | \_\_main\_\_.py |
| 3.4 | **Limpar `ApplicationBootstrapper`** — remover imports e instanciações de legacy coordinators/orchestrators | application_bootstrapper.py |
| 3.5 | **Mover `analysis_coordinator.py`** de `core/` para `coordinators/` | Coerência de pacotes |
| 3.6 | **Mover `video_orchestrator.py`** de `core/` para `coordinators/` ou eliminar | Coerência de pacotes |
| 3.7 | **Concluir migração EventBus** — escolher v1 ou v2, depreciar o outro com timeline, adicionar `DeprecationWarning` | event_bus.py, event_bus_v2.py |

---

### Fase 4 — Decomposição de God Classes (O maior ganho de manutenibilidade)

Depende da Fase 3 (base de herança unificada).

| # | Ação | De | Para | Target |
|---|------|----|------|--------|
| 4.1 | **Decompor `ProcessingCoordinator`** (5.433 linhas) | 1 ficheiro, 113 métodos | `VideoProcessingCoordinator`, `MultiAquariumCoordinator`, `SequentialProcessingCoordinator`, `ReportGenerationCoordinator`, `ProgressTrackingCoordinator` | <800 linhas cada |
| 4.2 | **Decompor `ProjectManager`** (3.227 linhas) | 1 ficheiro, 95 métodos | Verificar se delegação para `ZoneManager`, `VideoManager`, `AssetManager` existentes está completa. Extrair responsabilidades restantes. | <800 linhas |
| 4.3 | **Decompor `Detector`** (2.605 linhas) | 1 ficheiro | Separar: `SingleDetector`, `MultiAquariumDetector`, `DetectionPostProcessor`, `ZoneScaler` | <700 linhas cada |
| 4.4 | **Decompor `gui.py`** (2.250 linhas, 143 métodos) | 1 ficheiro | Continuar extração de concerns para `ui/components/` — identificar clusters de métodos relacionados | <800 linhas |
| 4.5 | **Decompor `canvas_manager.py`** (2.152 linhas) | 1 ficheiro | Separar rendering, event handling, e state | <700 linhas cada |
| 4.6 | **Decompor `project_view_manager.py`** (2.130 linhas) | 1 ficheiro | Separar tree management, report display, e navigation | <700 linhas cada |
| 4.7 | **Decompor session_coordinator.py** (2.111 linhas) | 1 ficheiro | Separar recording session, live camera session, batch session | <700 linhas cada |
| 4.8 | **Decompor `reporter.py`** (1.749 linhas) | 1 ficheiro | Separar `WordReporter`, `ExcelReporter`, `ParquetSummaryReporter` | <600 linhas cada |
| 4.9 | **Decompor hardware_coordinator.py** (1.692 linhas) | 1 ficheiro | Separar detector setup, model diagnostics, hardware detection | <600 linhas cada |
| 4.10 | **Sub-pacotizar `core/`** (40+ ficheiros) | 1 pacote flat | `core/project/`, `core/detection/`, `core/video/`, `core/recording/`, `core/services/` | Agrupamento por domínio |

---

### Fase 5 — Segurança e Thread Safety

Pode ser paralelizada com a Fase 4.

| # | Ação | Severidade | Ficheiro(s) |
|---|------|-----------|-------------|
| 5.1 | **Substituir `pickle.load` por JSON/msgpack** em cache de métricas | MÉDIA | metrics_cache.py |
| 5.2 | **Adicionar `threading.Lock` ao `Recorder.detection_data`** — proteger append/flush entre threads | MÉDIA | recorder.py |
| 5.3 | **Tornar escrita Parquet atômica** — escrever em ficheiro temporário + rename | BAIXA-MÉDIA | recorder.py |
| 5.4 | **Corrigir `tempfile.NamedTemporaryFile(delete=False)`** — garantir cleanup no caller | BAIXA | reporter.py |
| 5.5 | **Adicionar `pip-audit` ou `safety` ao CI** | MÉDIA | ci.yml |
| 5.6 | **Remover imports diretos de `cv2`, `numpy`, `pandas`, `shapely` em coordinators** — delegar para services | BAIXA | processing_coordinator.py |

---

### Fase 6 — Type System e Qualidade Estática

Pode ser paralelizada com Fases 4-5.

| # | Ação | Contagem | Ficheiro(s) Prioritários |
|---|------|----------|--------------------------|
| 6.1 | Resolver `# type: ignore` em settings.py | 16 instâncias | Corrigir type annotations ou usar overloads |
| 6.2 | Resolver `# type: ignore` em \_\_main\_\_.py | 14 instâncias | Refatorar two-phase init que causa a maioria |
| 6.3 | Resolver `# type: ignore` em ultralytics_detector.py | 12 instâncias | Criar type stubs ou protocols para Ultralytics API |
| 6.4 | Resolver `# type: ignore` em zone_management_facade.py | 10 instâncias | — |
| 6.5 | Resolver `# type: ignore` em decorators.py | 8 instâncias | — |
| 6.6 | Varrer os ~55 restantes por prioridade de risco | ~55 | Codebase inteiro |
| 6.7 | **Resolver inicialização two-phase** do `MainViewModel` (`__new__` + proxy) — eliminar dependência circular ViewModel ↔ GUI | 1 | \_\_main\_\_.py |
| 6.8 | **Eliminar pattern `controller=None`** em serviços — completar DI migration | Vários | `RecordingService`, `LiveCameraService`, etc. |
| 6.9 | **Reduzir `BootstrapResult` de 24 campos** — split em domain-specific result objects | 1 | application_bootstrapper.py |

---

### Fase 7 — Performance

Depende parcialmente da Fase 4 (decomposição de `Detector` facilita 7.1 e 7.2).

| # | Ação | Impacto Estimado | Ficheiro(s) |
|---|------|-----------------|-------------|
| 7.1 | **Treinar/converter modelo detection-only** — eliminar desperdício de segmentação | 30-50% speedup em inferência | Modelo + detector.py |
| 7.2 | **Implementar batch inference real** nos plugins | 2-4x throughput offline | ultralytics_detector.py, openvino_detector.py |
| 7.3 | **Usar `multiprocessing.shared_memory`** para preview frames em vez de Queue pickle | ~3-5MB/frame economizados na serialização | processing_worker.py |
| 7.4 | **Vetorizar `get_angular_velocity()`** — eliminar Python loop | Significativo para trajetórias 100k+ pontos | behavior.py |
| 7.5 | **Substituir list-of-dicts por buffers colunares** no flush Parquet | Reduz overhead de conversão a cada 500 rows | recorder.py |
| 7.6 | **Adicionar cache para ROI point-in-polygon** no detection loop | Evita recomputação em frames consecutivos | detector.py |
| 7.7 | **Substituir caches hand-rolled por `functools.lru_cache`** onde aplicável | Reduz código custom, melhora testabilidade | `wizard_service.py`, `video_manager.py` |
| 7.8 | **Adicionar model warm-up** — primeira inferência YOLO é mais lenta por JIT | Elimina latência do primeiro frame | ultralytics_detector.py |

---

### Fase 8 — Documentação e Padronização

Pode ser executada a qualquer momento, mas faz mais sentido após as refatorações.

| # | Ação | Detalhe |
|---|------|---------|
| 8.1 | **Padronizar idioma dos docstrings** — English para todo código técnico, Portuguese apenas em wiki | Inconsistência entre `gui.py` (PT) e `coordinators/` (EN) |
| 8.2 | **Elevar coverage gates** — Linux 55% → 70%, Windows 25% → 50% | CI coverage gates modestos para software científico |
| 8.3 | **Adicionar property-based testing** com Hypothesis (já é dependência dev) | Nenhum uso atual de fuzzing/property-based testing |
| 8.4 | **Documentar decisão de EventBus** — ADR para escolha v1 vs v2 | decisions |
| 8.5 | **Atualizar `SYSTEM_INTEGRATION_MAP`** após todas as decomposições | Refletir novos coordinators e fluxos |

---

### Resumo Visual — 42 Ações em 9 Fases

```
Fase 0 ─── 3 ações ──────── (triviais, 1h total)
  │
Fase 1 ─── 8 ações ──────── (except…pass, 1-2 dias)
  │
Fase 2 ─── 7 ações ──────── (except Exception, 2-3 dias)
  │
Fase 3 ─── 7 ações ──────── (unificação estrutural, 2-3 dias)
  │
Fase 4 ─── 10 ações ─────── (decomposição god classes, 2-3 semanas) ◄ MAIOR ESFORÇO
  │         │
  │    Fase 5 ── 6 ações ── (segurança, 3-4 dias) ─────── paralela
  │    Fase 6 ── 9 ações ── (type system, 1 semana) ────── paralela
  │         │
Fase 7 ─── 8 ações ──────── (performance, 1-2 semanas)
  │
Fase 8 ─── 5 ações ──────── (documentação, 2-3 dias)

TOTAL: 42 ações │ ~6-8 semanas com 1 engenheiro
                │ ~3-4 semanas com 2 engenheiros (Fases 4+5+6 paralelas)
```

As fases 0→3 são sequenciais e obrigatórias nessa ordem. A partir da fase 4, os branches paralelos (5, 6) podem ser executados simultaneamente por engenheiros diferentes. A fase 7 depende parcialmente da 4. A fase 8 fecha o ciclo.
