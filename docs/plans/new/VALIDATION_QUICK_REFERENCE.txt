================================================================================
ZEBTRACK-AI VALIDATION ANALYSIS - QUICK REFERENCE
================================================================================

TOTAL ISSUES FOUND: 16+ critical validation gaps

================================================================================
CRITICAL ISSUES (Must Fix)
================================================================================

1. SETTINGS.PY - Line 63-68: Camera dimensions missing bounds
   - desired_width: no bounds (should be gt=0, le=7680)
   - desired_height: no bounds (should be gt=0, le=4320)
   - Impact: Negative/zero dimensions crash OpenCV, large values exhaust memory

2. SETTINGS.PY - Line 210: FPS missing bounds
   - fps: no bounds (should be gt=0, le=120)
   - Impact: FPS=0 causes division by zero, negative FPS crashes OpenCV

3. SETTINGS.PY - Line 211-220: Processing interval missing bounds
   - processing_interval: no bounds (should be ge=1, le=1000)
   - processing_offset: no bounds (should be ge=0)
   - Impact: Modulo division by zero in video_processing_service.py line 322

4. SETTINGS.PY - Line 165: Model path not validated
   - path: no file existence check
   - Impact: File not found crashes at first detection use (not at load time)

5. SETTINGS.PY - Line 136: Arduino baud_rate missing bounds
   - baud_rate: no bounds (should be ge=300, le=2000000)
   - Impact: Invalid baud rates crash serial connection

6. WIZARD/MODELS.PY - Line 121: Detector name not validated
   - detector_name: accepts empty/invalid values
   - Impact: Silent detector lookup failure later

7. WIZARD/MODELS.PY - Line 139-147: Video file paths not validated
   - Only checks for empty strings
   - Impact: Non-existent files pass validation, fail hours later during batch
   - Missing: file existence check, file extension validation, is_file() check

8. PROJECT_MANAGER.PY - Line 862-865: Calibration params missing bounds
   - num_aquariums: no bounds (should be 1-1000)
   - animals_per_aquarium: no bounds (should be 1-1000)
   - aquarium_width_cm: allows 0 (should be gt=0, le=500)
   - aquarium_height_cm: allows 0 (should be gt=0, le=500)
   - Impact: Division by zero in calibration calculations, invalid project state

9. PROJECT_MANAGER.PY - Line 871-872: Frame intervals missing bounds
   - analysis_interval_frames: no bounds (should be 1-100)
   - display_interval_frames: no bounds (should be 1-100)
   - Impact: Zero values cause modulo errors in video processing

10. PROJECT_MANAGER.PY - Line 873: Camera index missing lower bound
    - camera_index: no lower bound check (should be ge=0, le=100)
    - Impact: Negative indices try to open invalid cameras

================================================================================
HIGH PRIORITY FIXES
================================================================================

11. PROJECT_MANAGER.PY - Line 902-903: Create project doesn't validate video files exist
    - Only checks if list is None
    - Impact: Bad paths discovered hours later during batch processing

12. PROJECT_MANAGER.PY - Line 1141-1144: Loaded camera_index not bounds-checked
    - Corrupted config with camera_index=-50 would load unchecked
    - Impact: Silent failure when camera opens later

13. SETTINGS.PY - Line 600-641: Cross-field validation incomplete
    - No check for camera aspect ratio extremes (e.g., 0.01:1)
    - No model path file existence validation
    - Impact: Silent data corruption with incompatible settings

================================================================================
WIZARD/MODELS.PY - MEDIUM PRIORITY
================================================================================

14. Line 22-24: recording_duration_s allows 0
    - ge=0 allows zero even when timed_recording=true
    - Should be: gt=0 when use_timed_recording=true

15. Line 122-133: Threshold fields optional when detector selected
    - confidence_threshold/nms_threshold allow None after detector picked
    - Impact: Partial threshold setup creates inconsistent state

================================================================================
RECOMMENDED FIX ORDER
================================================================================

PHASE 1 (CRITICAL - Do first):
  [ ] Add bounds to camera dimensions (settings.py)
  [ ] Add bounds to fps (settings.py)
  [ ] Add bounds to processing_interval/offset (settings.py)
  [ ] Add model path file existence check (settings.py)
  [ ] Add bounds to calibration params (project_manager.py)
  [ ] Add bounds to frame intervals (project_manager.py)

PHASE 2 (HIGH - Prevent silent failures):
  [ ] Add video file validation (wizard/models.py)
  [ ] Add detector name validation (wizard/models.py)
  [ ] Add camera_index bounds check in migration (project_manager.py)
  [ ] Add camera_index lower bound (project_manager.py)

PHASE 3 (MEDIUM - Improve robustness):
  [ ] Add cross-field validation (settings.py)
  [ ] Add Arduino port format validation (settings.py)
  [ ] Fix recording_duration_s zero case (wizard/models.py)
  [ ] Add threshold validator (wizard/models.py)

================================================================================
VALIDATION PATTERNS TO USE
================================================================================

For numeric bounds:
  Field(..., gt=0, le=1000)  # Greater than 0, less than or equal to 1000
  Field(..., ge=1, le=1000)  # Greater than or equal to 1

For string validation:
  Field(..., min_length=1, max_length=255, pattern=r'^[a-zA-Z0-9_]+$')

For field validators:
  @field_validator("field_name")
  @classmethod
  def validate_field_name(cls, v):
      if invalid:
          raise ValueError("description")
      return v

For cross-field validation:
  @model_validator(mode="after")
  def validate_cross_fields(self):
      if self.field1 > self.field2:
          raise ValueError("field1 must be <= field2")
      return self

================================================================================
DETAILED ANALYSIS: See VALIDATION_ANALYSIS.md (587 lines)
================================================================================

Each issue includes:
  - File path and line numbers
  - Current code showing the problem
  - What could go wrong (failure scenarios)
  - Suggested fix with code examples
  - Severity level and impact assessment

