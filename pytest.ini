[pytest]
addopts = -ra
testpaths =
    tests
norecursedirs =
    tests/manual

# Custom markers for test categorization
markers =
    gui: Tests that require Tkinter GUI components (run sequentially)
    slow: Tests that take significant time (>1s) to complete
    integration: Integration tests that test multiple components together
    unit: Unit tests that test a single component in isolation
    ttkbootstrap_singleton: Tests affected by ttkbootstrap Style singleton (run in isolation)

# pytest-xdist configuration
# GUI tests must run sequentially to avoid Tkinter conflicts
# Use: pytest -m "not gui" for fast parallel execution
# Use: pytest -m gui for sequential GUI tests